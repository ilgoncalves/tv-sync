import { FC } from 'react';
import { Div } from 'react-native-magnus';
import { RequestEmailProps } from './types';

const RequestEmail: FC<RequestEmailProps> = ({}) => {
  return <Div></Div>;
};

export { RequestEmail };
