import { ThemeType } from 'react-native-magnus';
import { Input } from './Input';
import { Text } from './Text';
import { Button } from './Button';

export const components: ThemeType['components'] = {
  Text,
  Button,
  Input,
};
